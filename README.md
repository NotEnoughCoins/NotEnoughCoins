/neckey <key> - Set your API key for the mod.
/flip - Returns a list of suggested items you could flip.
/necdata - Returns a longer list of flippable items. Only works after /flip has returned data at least once.